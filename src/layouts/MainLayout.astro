---
import Footer from '@components/Footer.astro';
import Navbar from '@components/Navbar.astro';
import Sidebar from '@components/Sidebar.astro';
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';

const { lang = 'fr' } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <script is:inline>
      // 1. Define the theme detection function globally
      function getTheme() {
        const storedTheme = localStorage.getItem('theme');
        if (storedTheme) {
          return storedTheme;
        }
        return window.matchMedia('(prefers-color-scheme: dark)').matches
          ? 'coffee'
          : 'caramellatte';
      }

      // 2. Apply it IMMEDIATELY (before page paint)
      const theme = getTheme();
      document.documentElement.setAttribute('data-theme', theme);

      // 3. Ensure it persists after View Transitions
      document.addEventListener('astro:after-swap', () => {
        document.documentElement.setAttribute('data-theme', getTheme());
      });
    </script>
    <title>Portfolio - Lohan Boëglin</title>
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <ClientRouter />
  </head>

  <body class="bg-base-100 text-base-content">
    <div class="drawer lg:drawer-open">
      <input id="my-drawer" type="checkbox" class="drawer-toggle" />
      <Sidebar transition:persist="true" />
      <div class="drawer-content flex flex-col">
        <Navbar transition:persist="true" />
        <main class="p-8 md:p-12 max-w-5xl mx-auto w-full">
          <slot />
        </main>
        <Footer transition:persist="true" />
      </div>
    </div>
  </body>
</html>
